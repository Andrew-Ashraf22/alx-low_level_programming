#include "main.h"
/**
*_abs - get the absloute value of a number
*@n: number to be checked
*Return: n after getting abslouted
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-1 * n);
}
